name: lifegoals
description: Flutter lifegoals app
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  bloc: ^8.1.2
  cloud_firestore: ^4.8.4
  firebase_auth: ^4.7.2
  firebase_core: ^2.15.0
  firebase_ui_auth: ^1.6.4
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_launcher_icons: ^0.13.1
  flutter_localizations:
    sdk: flutter
  freezed_annotation: ^2.4.1
  go_router: ^10.0.0
  intl: any # uses transitive dependency from flutter_localizations
  json_annotation: ^4.8.1
  logging: ^1.2.0
  mobile_scanner: ^3.3.0
  uuid: ^3.0.7

dev_dependencies:
  bloc_test: ^9.1.4
  build_runner: ^2.4.6
  fake_cloud_firestore: ^2.4.1+1
  flutter_test:
    sdk: flutter
  freezed: ^2.4.1
  #injectable_generator: ^2.1.3
  json_serializable: ^6.7.1
  melos: ^3.1.1
  mocktail: ^1.0.0
  very_good_analysis: ^5.0.0+1

flutter:
  uses-material-design: true

# to generate call:
# flutter pub run flutter_launcher_icons
flutter_icons:
  image_path: "assets/icon.png"
  android: true
  ios: true

# use overrides if there's conflicts in future..
# https://github.com/firebase/flutterfire/issues/10199
#dependency_overrides:
#  intl: ^0.18.0
